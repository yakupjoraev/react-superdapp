const balances = [{
    summ: 0,
    sol: {
        balance: 0,
        usd: 0,
    },
    sol_usd: 0,
}];

const percents = [{
    sol: 0,
}]

const prices = [{
    sol: 0,
}]

const defaults = { balances, percents, prices }

export default defaults;